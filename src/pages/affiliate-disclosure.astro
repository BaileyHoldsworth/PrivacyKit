---
import ArticleLayout from '../layouts/ArticleLayout.astro';
import { breadcrumbLd } from '../lib/jsonld';
const path = '/affiliate-disclosure/';
const crumbs = [{ name: 'Home', path: '/' }, { name: 'affiliate-disclosure', path }];
---
<ArticleLayout
  title="PLACEHOLDER — affiliate-disclosure | PrivacyKit"
  description="Placeholder description for the affiliate-disclosure page that is at least one hundred and twenty characters long so it satisfies the schema during the build."
  path={path}
  h1="affiliate-disclosure"
  crumbs={crumbs}
  jsonLd={[breadcrumbLd(crumbs)]}
>
  <p>Placeholder. Real content written in Phase D.</p>
</ArticleLayout>
