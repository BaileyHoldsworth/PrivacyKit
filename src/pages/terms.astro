---
import ArticleLayout from '../layouts/ArticleLayout.astro';
import { breadcrumbLd } from '../lib/jsonld';
const path = '/terms/';
const crumbs = [{ name: 'Home', path: '/' }, { name: 'terms', path }];
---
<ArticleLayout
  title="PLACEHOLDER — terms | PrivacyKit"
  description="Placeholder description for the terms page that is at least one hundred and twenty characters long so it satisfies the schema during the build."
  path={path}
  h1="terms"
  crumbs={crumbs}
  jsonLd={[breadcrumbLd(crumbs)]}
>
  <p>Placeholder. Real content written in Phase D.</p>
</ArticleLayout>
